import { FC } from 'react';
import styled from '@emotion/styled';

const Wrapper = styled.div``;

interface PartnersProps {}

export const Partners: FC<PartnersProps> = () => {
  return <Wrapper>PARTNERS</Wrapper>;
};
