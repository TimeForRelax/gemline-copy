import { FC } from 'react';
import styled from '@emotion/styled';

const Wrapper = styled.div``;

interface InvestmentProps {}

export const Investment: FC<InvestmentProps> = () => {
  return <Wrapper>INVESTMENT</Wrapper>;
};
